{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
        {{ form_row(form.name) }}
        {{ form_row(form.brand) }}
        {{ form_row(form.description) }}
        {{ form_row(form.price) }}
        {{ form_row(form.type) }}
        <button type="submit">Valider</button>
    {{ form_end(form) }}
{% endblock %}

{% block title %}

{% endblock %}